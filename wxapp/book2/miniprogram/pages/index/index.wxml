<view wx:for="{{phone_list}}" wx:for-item="phone">
  <image class="group-content-item-img" src="{{phone.image}}" />
  
  <van-card
    thumb="{{phone.image}}"
    price="{{phone.price}}"
    title="{{phone.title}}"
    
   >
  </van-card>
</view>
