## 回调
从回调和回调地狱说起
1、回调嵌套
不好的地方：
- 错误无法捕获
- 回调函数能否执行不清楚

回调地狱：

1、难以复用，回调地狱是有顺序的，当他确定完后，如果想对某些环节进行复用也很困难。
2、因为是异步的缘故，使用 try catch 语句也无法直接捕获错误。（这个函数到promise也没有解决，到 Generator async出现）


## promise
